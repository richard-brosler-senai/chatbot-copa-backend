[project]
name = "copa-chatbot-backend"
version = "1.0.0"
description = "Backend Flask para chatbot especialista em Copas do Mundo usando Groq."
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1,<4",
    "flask-cors>=5,<7",
    "groq>=0.31,<1",
    "python-dotenv>=1.1,<2",
]

[dependency-groups]
dev = []
